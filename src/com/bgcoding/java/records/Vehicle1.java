package com.bgcoding.java.records;

// Records generate the ff: variables in the class, constructor, equals(), hashCode(), toString(), and getters
public record Vehicle1(String brand, String licensePlate) { }
